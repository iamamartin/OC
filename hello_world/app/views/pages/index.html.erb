<h1>Home page</h1>
<%


response = HTTParty.get('https://mdms.owenscorning.com/api/v1/product/shingles?zip='+@zip.to_s)
body = JSON.parse(response.body)

#puts body[@line]["name"]
#body[@line]["shingle_colors"].each do |key, array|
#  puts key["name"]
#  puts key["swatch_160x160_url"]
#end

colornames = body[@line]['shingle_colors'].map{|item| item['name']}
swatches = body[@line]['shingle_colors'].map{|item| item['swatch_160x160_url']}
heros = body[@line]['shingle_colors'].map{|item| item['swatch_768x485_url']}




# newline(this)
%>
<%= select("body[0]", "['uid']", body.map {|v| [ v['name'], v['uid'] ] }, {}, { :onChange =>'subline(this);' }) %>

<%= body[@line]["name"] %>

<%= carousel_for(heros) %>

<%= gallery_for(swatches, colornames) %>


